<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <!-- Allow cleartext traffic to localhost/emulator for development -->
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">10.98.82.43</domain>
        <!-- Add your computer's actual IP if testing on physical device -->
        <!-- <domain includeSubdomains="false">192.168.1.XXX</domain> -->
    </domain-config>

    <!-- For all other domains, require HTTPS -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <!-- Trust system certificate authorities -->
            <certificates src="system"/>
        </trust-anchors>
    </base-config>
</network-security-config>